# Unpackarr Wrapper Configuration
# This wrapper runs the official Unpackerr binary with enhanced health monitoring
# Copy this file to .env and configure for your environment

# =============================================================================
# WRAPPER SETTINGS
# =============================================================================

# HTTP server port for wrapper health and metrics endpoints
# Default: 9092
HEALTH_PORT=9092

# Log level for wrapper logs: DEBUG, INFO, WARN, ERROR
# Default: INFO
LOG_LEVEL=INFO

# =============================================================================
# UNPACKERR CONFIGURATION (Passthrough)
# =============================================================================
# All other environment variables are passed through to the Unpackerr binary.
# See official Unpackerr documentation for full configuration options:
# https://unpackerr.zip/docs/install/configuration/

# Common Unpackerr environment variables:

# --- Extraction Settings ---
# UN_PARALLEL=1                    # Number of concurrent extractions
# UN_DELETE_ORIG=false             # Delete archives after extraction
# UN_PASSWORDS=                    # Comma-separated extraction passwords

# --- Sonarr (TV Shows) ---
# UN_SONARR_0_URL=http://sonarr:8989
# UN_SONARR_0_API_KEY=your-api-key
# UN_SONARR_0_PATHS_0=/downloads
# UN_SONARR_0_PROTOCOLS=torrent,usenet
# UN_SONARR_0_TIMEOUT=30s
# UN_SONARR_0_DELETE_DELAY=5m

# --- Radarr (Movies) ---
# UN_RADARR_0_URL=http://radarr:7878
# UN_RADARR_0_API_KEY=your-api-key
# UN_RADARR_0_PATHS_0=/downloads
# UN_RADARR_0_PROTOCOLS=torrent,usenet
# UN_RADARR_0_TIMEOUT=30s
# UN_RADARR_0_DELETE_DELAY=5m

# --- Lidarr (Music) ---
# UN_LIDARR_0_URL=http://lidarr:8686
# UN_LIDARR_0_API_KEY=your-api-key
# UN_LIDARR_0_PATHS_0=/downloads

# --- Readarr (Books/Audiobooks) ---
# UN_READARR_0_URL=http://readarr:8787
# UN_READARR_0_API_KEY=your-api-key
# UN_READARR_0_PATHS_0=/downloads

# --- Folder Watching (Standalone Mode) ---
# UN_FOLDER_0_PATH=/downloads      # Watch folder for archives
# UN_FOLDER_0_EXTRACT_PATH=/downloads  # Extract destination

# --- Webhook Notifications ---
# UN_WEBHOOK_0_URL=https://discord.com/api/webhooks/xxx
# UN_WEBHOOK_0_NICKNAME=Unpackerr
# UN_WEBHOOK_0_CHANNEL=downloads
# UN_WEBHOOK_0_TIMEOUT=10s
# UN_WEBHOOK_0_SILENT=false
# UN_WEBHOOK_0_EVENTS=0            # Bitmask: 1=queued, 2=extracting, 4=extracted, 8=deleted, 16=error

# --- Web Server (Metrics) ---
# UN_WEBSERVER_METRICS=true        # Enable Prometheus metrics on port 5656
# UN_WEBSERVER_LISTEN_ADDR=0.0.0.0:5656

# =============================================================================
# NOTES
# =============================================================================
#
# Wrapper Ports:
#   - 9092: Wrapper health endpoints (/ping, /health, /ready, /status, /metrics)
#   - 5656: Unpackerr metrics endpoint (if UN_WEBSERVER_METRICS=true)
#
# For complete Unpackerr configuration options, see:
# https://unpackerr.zip/docs/install/configuration/
#
# Path Configuration:
#   Ensure download paths are consistent between Docker mounts and app settings.
#   Example: If Sonarr downloads to /data/torrents, mount that to the container.
#
